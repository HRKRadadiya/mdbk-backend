
export default {
    side_character: {
        rules: {
            // registration_type: 'required|integer|in:1,2',
        }
    },
    client: {
        rules: {
            // registration_type: 'required|integer|in:1,2',
        }
    },
}